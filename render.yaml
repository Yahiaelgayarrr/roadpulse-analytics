services:
  - type: web
    name: roadpulse-analytics
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    healthCheckPath: /
